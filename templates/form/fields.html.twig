

{% block _ticket_order_visitDate_row %}
        <div class="col-md-4">
            {{ block('form_row') }}
        </div>
{%  endblock %}

{% block _ticket_order_duration_row %}
    <div class="col-md-4">
        {{ block('form_row') }}
    </div>
{%  endblock %}

{% block _ticket_order_bookingEmail_row %}
    <div class="col-md-4">
        {{ block('form_row') }}
    </div>
{%  endblock %}

{#{% block _ticket_order_ticket_visitorFirstName_row %}#}
    {#<div class="row">#}
        {#<div class="col-md-2">#}
            {#{{ block('form_row') }}#}
        {#</div>#}
    {#</div>#}
{#{%  endblock %}#}


{# Overide des selects du widget birthdayType #}
{% block date_widget -%}
    {%- if widget == 'single_text' -%}
        {{- block('form_widget_simple') -}}
    {%- else -%}
        {%- set attr = attr|merge({class: (attr.class|default('') ~ ' form-inline')|trim}) -%}
        {%- if datetime is not defined or not datetime -%}
            <div {{ block('widget_container_attributes') -}}>
        {%- endif %}
        {%- if label is not same as(false) -%}
            <div class="sr-only">
                {{ form_label(form.year) }}
                {{ form_label(form.month) }}
                {{ form_label(form.day) }}
            </div>
        {%- endif -%}

        {{- date_pattern|replace({
            '{{ year }}': form_widget(form.year, {'attr' : {'class' : 'form-control-sm'}}),
            '{{ month }}': form_widget(form.month, {'attr' : {'class' : 'form-control-sm'}}),
            '{{ day }}': form_widget(form.day, {'attr' : {'class' : 'form-control-sm'}}),
        })|raw -}}
        {%- if datetime is not defined or not datetime -%}
            </div>
        {%- endif -%}
    {%- endif -%}
{%- endblock date_widget %}



{# Overide of tickets collection data-prototype  #}
{% block _ticket_order_tickets_entry_row %}
    <div class="ticketSolo col-md-4">
        <div class="card card-body bg-light">
        <h3></h3>
        {{ form_row(form.visitorFirstName) }}
        {{ form_row(form.visitorName) }}
        {{ form_row(form.visitorBirthDate) }}
        {{ form_row(form.discount) }}
        {{ form_row(form.country) }}
        </div>
    </div>
{% endblock %}


